import { Button } from "@mui/material";
import React from "react";

const Feedback = (props) => {
  return (
    <Button variant="outlined" color="secondary" sx={{ marginBottom: "8px" }}>
      Feedback
    </Button>
  );
};

export default Feedback;
